---
description: Memory dream pass — judge undreamed days, promote durable signal to long-term, then sweep
argument-hint: "[YYYY-MM-DD]"
---

Invoke the linggen skill (Skill tool) and treat this message as `/linggen dream $ARGUMENTS` — Dream mode. Follow the skill's Dream runbook (`references/dream-flow.md`): if an argument gives a `YYYY-MM-DD` date, remember only that day; otherwise process all undreamed days oldest-first (`ling-mem days --undreamed`), then run the forget sweep.
