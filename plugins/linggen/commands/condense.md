---
description: Collapse stale same-subject chains in long-term memory into one current-truth row
---

Invoke the linggen skill (Skill tool) and treat this message as `/linggen condense` — Condense mode. Follow the skill's Condense runbook (`references/condense-flow.md`): back up first (`ling-mem export`), scan chains via `ling-mem chains --derived-only`, collapse each into one current-truth row (add the survivor, then delete members).
